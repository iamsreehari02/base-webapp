import React from 'react'

const CommonButton = ({label}) => {
  return (
    <button className='bg-customBlue px-10 py-2 text-white rounded-lg w-full'>{label}</button>
  )
}

export default CommonButton
